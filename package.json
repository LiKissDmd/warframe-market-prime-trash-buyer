{
  "name": "warframe-market-prime-trash-buyer",
  "version": "0.0.1-dev.0",
  "description": "",
  "main": "dist/main.js",
  "type": "module",
  "types": "dist/main.d.ts",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc && node dist/main.js",
    "clean-build": "rm --recursive dist && npx tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
